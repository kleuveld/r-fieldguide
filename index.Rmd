--- 
title: "Reproducible and reusable social science with R: a field guide"
author: "Koen Leuveld-Postma"
date: "`r Sys.Date()`"
site: bookdown::bookdown_site
documentclass: book
#bibliography: [book.bib, packages.bib]
# url: your book url like https://bookdown.org/yihui/bookdown
# cover-image: path to the social sharing image like images/cover.jpg
description: |
  This page contains tips to make your R code reproducible.
link-citations: yes
github-repo: kleuveld/r_cheatsheet
---

```{r setup, include=FALSE}

options('box.path' = here::here())


knitr::opts_chunk$set(eval=TRUE, 
                      include = TRUE, 
                      echo=TRUE, 
                      warning=FALSE, 
                      error=TRUE, 
                      message=FALSE)

# to trick renv into keeping these in the lockfile
library(languageserver)
library(httpgd)

```

# Introduction

This website is intended as a quick reference for some techniques 
that I think people may need to keep their code reproducible
when cleaning, analyzing, or presenting data. 
The main audience is me, but I think it may be useful for other people as well,
especialy those doing work similar to me: 
analyzing household survey data.
It is not intended as a quick-start guide to R. 
For that, try 
[the R for Social Science Data Carpentry Workshop](https://datacarpentry.org/r-socialsci/)
, on which some of this website is based.


## Why reproducibility?

My work is often one-off: I analyze the data, write the report or paper, 
and never look back.
Why would I care about reproducibility then? 
The most important reason is about integrity:
for any research work, you need to be able to show how you arrived
at your results.
That means someone else should be able to take your code and data, 
and arrive at the same results.

For other people to make sense of your code, 
it should be well-organized and readable.
Such code has more advantages than just research integrity: 
because it is easier to understand,
it's easier to modify. 
It turns out that most of my work isn't *that* one-off, 
so easily modifiable code is very useful for future projects.
Making sure my code can easily be repurposed for other projects by changing a few
parameters is thus worthwhile just because of the time it saves me.
Moreover, such code is less likely to produce wrong results.
Errors are easier to spot, 
and the code tends to make fewer assumptions about the data, 
because it has to work with different datasets.

In short, 
non-reproducible code increases the risk of sloppy science.


## Packages used in this site

Throughout this site, I will mostly make use of `tidyverse` packages.
While there are alternatives,
such as `data.table` which is faster,
`tidyverse` makes writing and reading code easy and quick,
which is imortant from a reproducibility perspective.
Moreover, we are not so concerned with performance, 
since household survey datasets are almost never large enough for that matter.

To provide easy access to the pacakges used in this site, 
as well as all data,
everything is available on [github](https://github.com/kleuveld/r_cheatsheet).
So if you want all code and data follow the following steps:

- Open RStudio
- Click file, and select "New project..."
- Select Version Control, Git, and enter `https://github.com/kleuveld/r_cheatsheet` 
as the repository URL.
- The Project will open.
- In the console, type `renv::restore()` to install all packages.

(See how easy that was? Reproducible workflows, baby!)

## Road map

The site is organized in two parts:
the first cover advice on general best practices: 
project oriented workflows, general R techniques such as loops and functions that facilitate reproducibility and RMarkdown.
The chapters in part two each cover a part of the research cycle:
design, data cleaning and data analysis (including reporting the results). 
For each, there's specific code examples using survey data. 




